// Zunoora uses local JSON files for storage — no database needed.
// See Blanks/ directory for all data files.

console.log("Zunoora is fully offline.");
console.log("Data source: Blanks/ directory (local JSON files)");
console.log("Storage: Browser localStorage for user sessions and documents");
console.log("AI: Configured in-app via Settings > AI Provider");
